% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_image.R
\name{plot_image}
\alias{plot_image}
\title{Plot Image with Colony Overlays}
\usage{
plot_image(
  img,
  colony_data = NULL,
  show_colony_grid = FALSE,
  use_new_boxes = FALSE,
  show_center_points = FALSE,
  show_rowcol_labels = FALSE
)
}
\arguments{
\item{img}{\code{cimg}. Image object loaded by \code{imager::load.image()}.}

\item{colony_data}{\code{data.frame} or \code{NULL}. Colony data with coordinates and
bounding box information. If \code{NULL}, only the base image is plotted.}

\item{show_colony_grid}{\code{logical(1)}. If \code{TRUE}, displays colony bounding
boxes as red rectangles. Default is \code{FALSE}.}

\item{use_new_boxes}{\code{logical(1)}. If \code{TRUE}, uses normalized bounding boxes
(new_xl, new_xr, new_yt, new_yb) from \code{create_grid_boxes()}. If \code{FALSE},
uses original boxes (xl, xr, yt, yb). Default is \code{FALSE}.}

\item{show_center_points}{\code{logical(1)}. If \code{TRUE}, displays colony center
points as yellow dots. Default is \code{FALSE}.}

\item{show_rowcol_labels}{\code{logical(1)}. If \code{TRUE}, displays row-column
labels for each colony. Default is \code{FALSE}.}
}
\value{
Invisibly returns \code{NULL}. Plots are created as side effects.
}
\description{
Creates a visualization of an image with optional overlays showing colony
grids, center points, and row-column labels. Uses base R graphics for
memory-efficient plotting of large images.
}
