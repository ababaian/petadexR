% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_edge_intensity.R
\name{plot_edge_intensity}
\alias{plot_edge_intensity}
\title{Create Edge Intensity Histograms with Optional Normalization (for cimg objects)}
\usage{
plot_edge_intensity(
  img_cimg1,
  img_cimg2,
  edge_width = 150,
  img1_name = "Background",
  img2_name = "Washed",
  normalize_background = FALSE
)
}
\arguments{
\item{img_cimg1}{\code{cimg}. First image object (typically background)
loaded with \code{load_image()} from the imager package.}

\item{img_cimg2}{\code{cimg}. Second image object (typically washed)
loaded with \code{load_image()} from the imager package.}

\item{edge_width}{\code{numeric(1)}. Width of edge region in pixels from the
outer border. Default is 150.}

\item{img1_name}{\code{character(1)}. Display name for first image.
Default is "Background".}

\item{img2_name}{\code{character(1)}. Display name for second image.
Default is "Washed".}

\item{normalize_background}{\code{logical(1)}. If \code{FALSE}, compares original images.
If \code{TRUE}, shows normalization effect on second image. Default is \code{FALSE}.}
}
\value{
A \code{list} containing:
\itemize{
\item \code{plot}: ggplot2 comparison histogram
\item \code{statistics}: Statistical summaries of intensities
\item \code{normalized_image}: Normalized image cimg object (only if normalize_background = TRUE)
}
}
\description{
Generates histograms comparing edge pixel intensities between two images
with optional background normalization. Works with cimg objects from the
imager package loaded with load_image(). Can either compare original images
directly or show the effect of normalization on the second image. Useful
for visualizing background correction and intensity distributions.
}
