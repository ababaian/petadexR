% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_grid_boxes.R
\name{create_grid_boxes}
\alias{create_grid_boxes}
\title{Create Normalized Grid Boxes for Colonies}
\usage{
create_grid_boxes(colony_data, mode = "inside", gap_factor = 0.05)
}
\arguments{
\item{colony_data}{\code{data.frame}. Colony data containing at minimum columns
\code{x}, \code{y}, \code{size}, \code{row}, and \code{col}. The \code{size} column should represent
the area of each colony.}

\item{mode}{\code{character}. Either "inside" (default) or "outside".
\itemize{
\item "inside": Boxes fit within colony boundaries
\item "outside": Boxes based on inter-colony spacing
}}

\item{gap_factor}{\code{numeric}. Only used when mode = "outside".
Fraction of median distance to use as gap between boxes (default: 0.1)}
}
\value{
A \code{data.frame} containing the original colony data plus new columns:
\itemize{
\item \code{new_xl}, \code{new_xr}: Left and right x-coordinates of bounding box
\item \code{new_yt}, \code{new_yb}: Top and bottom y-coordinates of bounding box
\item \code{grid_box_size}: Area of the created square bounding box
\item \code{box_mode}: The mode used to create the boxes
}
}
\description{
Creates square bounding boxes for each colony based on their size (area) or
based on inter-colony distances. The function supports two modes:
\itemize{
\item "inside": Creates the largest square that fits within each colony (assuming circular)
\item "outside": Creates boxes based on median inter-colony distance with optional gaps
}
}
\details{
If there is no colony, the function will label the new coordinates as NA.
}
